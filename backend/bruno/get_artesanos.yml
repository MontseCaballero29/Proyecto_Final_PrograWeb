info:
  name: get_artesanos
  type: http
  seq: 4

http:
  method: GET
  url: https://talleresartesaniasoaxaca.dpdns.org/api/artesanos
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
