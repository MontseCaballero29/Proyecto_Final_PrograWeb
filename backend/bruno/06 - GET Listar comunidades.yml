info:
  name: 06 - GET Listar comunidades
  type: http
  seq: 6

http:
  method: GET
  url: https://talleresartesaniasoaxaca.dpdns.org/api/comunidades
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
