info:
  name: Login ADMIN
  type: http
  seq: 2

http:
  method: POST
  url: https://talleresartesaniasoaxaca.dpdns.org/api/auth/login
  body:
    type: json
    data: |-
      {
        "correo": "admin@aripo.gob.mx",
        "password": "Admin123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
