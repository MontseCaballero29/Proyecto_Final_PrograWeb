info:
  name: error_credenciales_401
  type: http
  seq: 7

http:
  method: POST
  url: http://54.83.75.25:8090/api/auth/login
  body:
    type: json
    data: |-
      {
        "correo": "admin@aripo.gob.mx",
        "password": "Incorrecta123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
