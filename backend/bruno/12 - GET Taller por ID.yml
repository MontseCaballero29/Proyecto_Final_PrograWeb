info:
  name: 12 - GET Taller por ID
  type: http
  seq: 12

http:
  method: GET
  url: https://talleresartesaniasoaxaca.dpdns.org/api/talleres/1
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
