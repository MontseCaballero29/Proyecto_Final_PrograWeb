info:
  name: 04 - GET Artesano por ID
  type: http
  seq: 4

http:
  method: GET
  url: https://talleresartesaniasoaxaca.dpdns.org/api/artesanos/1
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
