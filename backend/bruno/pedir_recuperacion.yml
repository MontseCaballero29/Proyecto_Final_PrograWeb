info:
  name: pedir_recuperacion
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:8090/api/auth/recuperar
  body:
    type: json
    data: |-
      {
        "correo": "admin@aripo.gob.mx"
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
