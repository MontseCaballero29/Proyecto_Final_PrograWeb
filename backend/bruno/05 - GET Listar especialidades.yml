info:
  name: 05 - GET Listar especialidades
  type: http
  seq: 5

http:
  method: GET
  url: https://talleresartesaniasoaxaca.dpdns.org/api/especialidades
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
