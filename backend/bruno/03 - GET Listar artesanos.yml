info:
  name: 03 - GET Listar artesanos
  type: http
  seq: 3

http:
  method: GET
  url: https://talleresartesaniasoaxaca.dpdns.org/api/artesanos
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
