info:
  name: post_registro
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8090/api/auth/register
  body:
    type: json
    data: |-
      {
        "nombre": "visitante_prueba",
        "correo": "visitante.demo@gmail.com",
        "password": "visiTante1234!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
