info:
  name: 07 - GET Usuarios disponibles para artesano
  type: http
  seq: 7

http:
  method: GET
  url: https://talleresartesaniasoaxaca.dpdns.org/api/usuarios/disponibles-artesano
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
